const initialState = {
  guest: false,
  user: true,
  admin: false
}

const login = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state
  }
}

export default login;