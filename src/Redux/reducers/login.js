const initialState = {
  guest: false,
  user: false,
  admin: true
}

const login = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default login;